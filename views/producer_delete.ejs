<!DOCTYPE html>
<html>
	<head>
		<title><%= title %></title>
		<link rel="stylesheet" href="/stylesheets/style.css" />
	</head>
	<body>
		<main class="main">
			<%- include('./partials/menu') %>
			<div class="content">
				<h1><%= title %></h1>
				<h2><%= producer.name %></h2>
				<% if(items.length) { %>
				<p>Items below are assigned to this producer</p>
				<ul>
					<% items.forEach(element => { %>
					<li><a href="<%= element.url %> "><%= element.name %> </a></li>
					<% }); %>
				</ul>
				<p>Are you sure you want to delete it?</p>
				<% } else { %>
				<p>There are no items in this producer</p>
				<% } %>
				<form action="" method="post">
					<input type="hidden" name="producerid" required="true" value="<%= producer._id %>" />
					<button type="submit">Delete</button>
				</form>
			</div>
		</main>
	</body>
</html>
